apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: ghost
spec:
  entryPoints:
    - synapse-secure
  routes:
  - match: Host(`53ll.goatchat.ca`)
    kind: Rule
    services:
    - name: ghost-53ll
      namespace: ghost
      port: 8080
  tls:
    certResolver: default
